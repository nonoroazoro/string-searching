const mocha = require("mocha");
const should = require("should");

const ss = require("../../");

describe("lib/index", () =>
{
    describe("1.boyer-moor", () =>
    {
        it("should find string ", (done) =>
        {
        });
    });
});